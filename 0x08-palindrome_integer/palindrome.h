#ifndef PAL_H
#define PAL_H
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>


int is_palindrome(unsigned long n);
int is_palindrome_recursion(unsigned long n, unsigned long *dupen);

#endif /* PAL_H */
